# Database
DATABASE_URL=postgresql+asyncpg://nelson:nelson@localhost:5432/nelson

# Auth
JWT_SECRET=change-me-in-production
FERNET_KEY=change-me-in-production

# Email — uses Mailpit (SMTP) locally, Resend in production
EMAIL_PROVIDER=smtp
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM=nelson@localhost
RESEND_API_KEY=

# Frontend
CORS_ORIGINS=["http://localhost:3000"]
MAGIC_LINK_BASE_URL=http://localhost:3000/login/verify

# Frontend public
NEXT_PUBLIC_API_URL=http://localhost:8000
